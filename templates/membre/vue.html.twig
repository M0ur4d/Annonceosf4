{% extends "base.html.twig"  %}
{% block stylesheets %}{% endblock %}
{% block body %}

    <div class="columns">
        <div class="column is-half"></div>
    </div>
    <br>
    <h1>FICHE Membre</h1>

    <h2>
        {% if app.user.civilite == "F" %}
            Bonjour, Madame
        {% elseif app.user.civilite == "M"%}
            Bonjour, Monsieur
        {% else %}
            Bonjour,
        {% endif %}

        {{ app.user.pseudo|upper }}</h2>

<div class="columns">
    <div class="column is-half">
    <p>ID : {{ app.user.id }}</p>
    <p>Email : {{ app.user.email }}</p>
    <p>Nom : {{ app.user.nom }}</p>
    <p>Prenom : {{ app.user.prenom }}</p>
    <p>Tel : {{ app.user.telephone }}</p>
{#    <p>Civilité :{{ app.user.civilite }}</p>#}
    <p>Inscrit depuis le {{ app.user.dateenregistrement|date('d/m/Y')}}</p>
{#    <p>Statut :  {% for statut in app.user.roles %}#}
{#                    {{ statut }}#}
{#                 {% endfor %}</p>#}
    </div>

    <div class="column is-half">
        {% for ann in mesannonces %}
            {% include "annonce/vignette.html.twig" %}
        {% endfor %}
    </div>
</div>

{% endblock %}

